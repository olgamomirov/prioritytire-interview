query CategoriesSlug {
  categories {
    items {
      name
      url_key
      level
      children {
        name
        url_key
        level
        children {
          name
          url_key
          level
          children {
            name
            url_key
            level
          }
        }
      }
    }
  }
}
